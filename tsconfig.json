{
    "compilerOptions": {
        "allowJs": true, // If you want to have js files in here, no problemo.
        "jsx": "react", // Need this for react support
        "outDir": "./dist/", // The folder to put the transpiled files in
        "sourceMap": true,  // Will generate a .map file which can help with debugging, stack traces, etc.
        "strict": true,  // --noImplicitAny, --noImplicitThis, --alwaysStrict, --strictBindCallApply, --strictNullChecks, --strictFunctionTypes and --strictPropertyInitialization.

        /** [succinct explanation of these](https://medium.com/@tommedema/typescript-confusion-tsconfig-json-module-moduleresolution-target-lib-explained-65db2c44b491) */
        "target": "es5",
        "module": "es6", 
    },
    "include": [
        "./src/**/*"
    ]
}
